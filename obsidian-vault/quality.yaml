# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-obsidian-vault,
# the LAST entry on quality-allowlist.txt — this backfill takes the Phase D
# program to 100%). obsidian_vault.ts is BYTE-FROZEN — every new suite drives
# model.methods.<m>.execute() against a fake context (fs methods over a
# Deno.makeTempDir vault) or a stubbed Deno.Command (the 11 CLI-only
# methods), characterizing already-shipped behavior. Seven latent bugs found
# during the round-1 adversarial/security plan review, plus one more (#8)
# found while writing the methods/coverage suites themselves, are pinned as
# characterization tests and cataloged in the LOCAL
# obsidian-vault-latent-bugs issue-lifecycle model (never the swamp.club Lab).
schemaVersion: 1
extension: obsidian-vault
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/obsidian_vault_test.ts
  methods:
    state: present
    files:
      - extensions/models/obsidian_vault_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/obsidian_vault_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/obsidian_vault_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/obsidian_vault_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: obsidian-vault bundles no Claude skill — no .claude/skills/obsidian-vault/SKILL.md exists in this repo
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
