# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-obsidian-vault,
# the LAST entry on quality-allowlist.txt — this backfill took the Phase D
# program to 100%). Eight latent bugs fixed in 2026.08.02.1 (CRLF-frontmatter
# data loss, ReDoS alternation guard, backslash traversal, digest/search byte
# + signalHits bounds, digest backend enforcement, calendar-date validation,
# trash overwrite, real setProperties/propertyRemove action) — former
# `pin: KNOWN BUG` characterization tests in the adversarial and coverage
# suites are now regression tests asserting the fixed behavior. History
# remains cataloged in the LOCAL obsidian-vault-latent-bugs issue-lifecycle
# model (never the swamp.club Lab). Ratchet numbers below are re-stamped from
# a real `swamp extension quality` run against 2026.08.02.1.
schemaVersion: 1
extension: obsidian-vault
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/obsidian_vault_test.ts
  methods:
    state: present
    files:
      - extensions/models/obsidian_vault_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/obsidian_vault_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/obsidian_vault_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/obsidian_vault_property_test.ts
watch:
  # Owned by Phase B (ext-quality-release-watch-soak): the executable
  # contract is scripts/lib/watch_schema.ts, consumed by the generic
  # .github/workflows/release-watch.yml. obsidian-vault's only versioned
  # upstream dependency is the yaml frontmatter-parser pin in
  # obsidian_vault.ts — the local vault filesystem it operates on has no
  # version to watch.
  state: present
  issueLabel: obsidian-vault-release-watch
  sources:
    - kind: npm
      package: yaml
      channel: latest
      pin:
        from: source
        file: extensions/models/obsidian_vault.ts
        pattern: 'npm:yaml@([0-9][^/"]*)'
        required: true
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: obsidian-vault bundles no Claude skill — no .claude/skills/obsidian-vault/SKILL.md exists in this repo
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
