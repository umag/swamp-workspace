# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-kaiten (wave 2a
# gap-check child of ext-quality-test-backfill). kaiten.ts is unmodified —
# every suite drives model.methods.<m>.execute() against a stubbed fetch +
# fake context, characterizing already-shipped read-only behavior.
schemaVersion: 1
extension: kaiten
tests:
  # contract-fixture is anchored by kaiten_test.ts (SDK surface: 7 read-only
  # method names, arg defaults, the pure resolveBase/backoffMs/slug
  # helpers), kept byte-unchanged. kaiten_contract_test.ts adds the
  # wire-format half from a synthetic, doc/schema-derived fixture corpus
  # (see fixtures/PROVENANCE.md) — the documented Space/Board/Column/Card
  # keysets, the bare-array list shape, the numeric `condition` field, and
  # offset/limit pagination params.
  contract-fixture:
    state: present
    files:
      - extensions/models/kaiten_test.ts
      - extensions/models/kaiten_contract_test.ts
  methods:
    state: present
    files:
      - extensions/models/kaiten_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/kaiten_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/kaiten_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/kaiten_property_test.ts
watch:
  state: backlog
  justification: no generalized release-watch wired yet — tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: live canary workflow not built yet — tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: kaiten bundles no Claude skill — a read-only REST wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
