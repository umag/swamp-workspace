# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-pihole (wave 1
# gap-check child of ext-quality-test-backfill). pihole is REMOVED from
# quality-allowlist.txt in the same change as these flips.
schemaVersion: 1
extension: pihole
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/pihole_dns_test.ts
      - extensions/models/pihole_test.ts
      - extensions/models/pihole_live_test.ts
  methods:
    state: present
    files:
      - extensions/models/pihole_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/pihole_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/pihole_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/pihole_property_test.ts
watch:
  state: backlog
  justification: no generalized release-watch wired yet — tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: live canary workflow not built yet — tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: this extension bundles no Claude skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
