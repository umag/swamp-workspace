# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-homeassistant, wave
# 2c full build of the extension-quality backfill program,
# ext-quality-test-backfill). Every suite drives model.methods.<m>.execute()
# against a stubbed fetch + a novel FakeWebSocket scripted-responder (the
# model's one WS path, fetchStatistics) + a fake context.
#
# homeassistant.ts v2026.08.01.1 folds in fixes for two HIGH findings from the
# local `homeassistant-latent-bugs` issue-lifecycle model (WS clean-close hang
# + latent msg.id non-correlation) — homeassistant.ts is NO LONGER unmodified;
# see CHANGELOG.md. All remaining MED/LOW findings from that model are still
# characterized (pinned), not fixed.
schemaVersion: 1
extension: homeassistant
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/homeassistant_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/homeassistant_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/homeassistant_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/homeassistant_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/homeassistant_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: homeassistant bundles no Claude skill — a bare REST/WS model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
