# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-telegram-send,
# wave-2a gap-check of the Phase D extension-quality backfill), then hardened
# in 2026.08.01.1 (telegram-send-hardening-richmessage-port): every suite
# drives model.methods.<m>.execute() against a stubbed fetch (+ a stubbed
# Deno.readFile for the multipart branch) and a fake context. The
# token-in-URL fetch-rejection redaction gap tracked below in prior
# revisions of this comment is RESOLVED as of 2026.08.01.1 — telegramJson
# and telegramMultipart now wrap fetch() and redact via redactToken before
# rethrowing; see CHANGELOG.md and the local issue-lifecycle model
# telegram-send-hardening-richmessage-port for the fix history.
schemaVersion: 1
extension: telegram-send
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/telegram_send_test.ts
  methods:
    state: present
    files:
      - extensions/models/telegram_send_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/telegram_send_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/telegram_send_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/telegram_send_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: telegram-send bundles no Claude skill — a bare Bot API send wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
