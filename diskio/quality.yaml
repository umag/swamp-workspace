# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
schemaVersion: 1
extension: diskio
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/diskio_test.ts
  methods:
    state: present
    files:
      - extensions/models/diskio_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/diskio_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/diskio_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/diskio_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: diskio bundles no Claude skill — it is a host probe driven by
      four model methods, and the README documents the two counters and the
      FUSE indirection that the methods exist to expose
ratchet:
  rubricVersion: 3
  baselinePercentage: 0
  label: unscored
