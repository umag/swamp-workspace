# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# HISTORICAL NOTE (as of the ext-quality-bf-livejournal-import backfill):
# backfilled to full five-suite compliance (wave-4 batch-4a child of the
# extension-quality backfill program, ext-quality-test-backfill).
# livejournal_import.ts was BYTE-FROZEN AT THAT TIME — every listed test file
# characterized/pinned already-shipped behavior (including 8 latent bugs
# tracked in the LOCAL livejournal-import-latent-bugs issue-lifecycle model,
# never filed to the swamp.club Lab); no production code changed and no
# manifest/model version bump in that backfill.
#
# THE FREEZE HAS SINCE BROKEN: 2026.07.31.1 (see CHANGELOG.md and the
# `ratchet` block below) fixes latent bug LB1 (SSRF), repairs the model
# upgrade chain, and bumps model.version/manifest.yaml. Production code DID
# change in that release -- this header describes the ORIGINAL backfill
# only, not the current state of livejournal_import.ts.
#
# 2026.08.01.1 (swamp-workspace #57, mirrors PR #56's obsidian-vault backend
# split) adds an optional headless vaultRoot filesystem backend to `import`.
# All five suites extended with vaultRoot coverage -- see CHANGELOG.md.
#
# 2026.08.02.1 fixes LB7 (operator folder/attachmentsFolder path traversal on
# the attachment disk write): attachDiskPath is now resolved through the
# already-copied string-level resolveVaultPath before the post loop, in BOTH
# the CLI-fallback and vaultRoot branches. LB7 is FIXED -- see CHANGELOG.md.
#
# 2026.08.02.2 fixes the six remaining latent bugs (LB2 YAML frontmatter
# injection, LB3 silent-empty success, LB4 no fetch/subprocess timeout, LB5
# unbounded pagination, LB6 fragile comment-JSON extraction, LB8 parseLjDate
# silent fallthrough), adding two backward-compatible global args (timeoutMs
# default 30000, maxPages default 1000). All eight originally-tracked latent
# bugs (LB1-LB8) are now FIXED -- see CHANGELOG.md.
#
# Role map (each test file assigned to exactly ONE honest role):
#   contract-fixture        -> livejournal_import_test.ts
#   methods                 -> livejournal_import_methods_test.ts
#   adversarial              -> livejournal_import_adversarial_test.ts
#   coverage                 -> livejournal_import_coverage_test.ts
#   property-invariant-flow -> livejournal_import_property_test.ts
schemaVersion: 1
extension: livejournal-import
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/livejournal_import_test.ts
  methods:
    state: present
    files:
      - extensions/models/livejournal_import_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/livejournal_import_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/livejournal_import_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/livejournal_import_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: livejournal-import bundles no Claude skill — a blog-export importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: >-
    Grade A — the model upgrade chain is repaired and stays continuous
    (2026.03.29.1 -> 2026.05.25.1 -> 2026.07.16.2 -> 2026.07.31.1 ->
    2026.08.01.1 -> 2026.08.02.1 -> 2026.08.02.2, final toVersion ===
    model.version) as part of the SSRF fix in livejournal-import-latent-bugs
    (2026.07.31.1), the swamp-workspace #57 vaultRoot change (2026.08.01.1,
    identity upgrade entry appended), the LB7 path-traversal fix
    (2026.08.02.1, identity upgrade entry appended), and the LB2/LB3/LB4/
    LB5/LB6/LB8 real-fix batch (2026.08.02.2, identity upgrade entry
    appended -- two new backward-compatible global args, no resource schema
    change). All eight originally-tracked latent bugs are now fixed; none
    remain UNFIXED/deferred. `swamp extension quality manifest.yaml --json`
    emits a real score: 14/14 points (100%), allPassed true, dependencyTrust
    passed (cheerio/domhandler both audited clean; weeklyDownloads 27.2M /
    99.8M respectively). Stamped directly from that tool output (run against
    this worktree after the LB2/3/4/5/6/8 fix batch), not guessed -- the
    score is unchanged from the prior (2026.08.02.1) stamp since the LB batch
    added no new runtime dependency (only a TEST-ONLY jsr:@std/yaml dev
    dependency, which the dependency-trust factor does not score).
