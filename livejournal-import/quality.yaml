# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-livejournal-import,
# wave-4 batch-4a child of the extension-quality backfill program,
# ext-quality-test-backfill). livejournal_import.ts is BYTE-FROZEN by this
# change — every listed test file characterizes/pins already-shipped behavior
# (including 8 latent bugs tracked in the LOCAL
# livejournal-import-latent-bugs issue-lifecycle model, never filed to the
# swamp.club Lab); no production code changed and no manifest/model version
# bump.
#
# Role map (each test file assigned to exactly ONE honest role):
#   contract-fixture        -> livejournal_import_test.ts
#   methods                 -> livejournal_import_methods_test.ts
#   adversarial              -> livejournal_import_adversarial_test.ts
#   coverage                 -> livejournal_import_coverage_test.ts
#   property-invariant-flow -> livejournal_import_property_test.ts
schemaVersion: 1
extension: livejournal-import
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/livejournal_import_test.ts
  methods:
    state: present
    files:
      - extensions/models/livejournal_import_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/livejournal_import_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/livejournal_import_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/livejournal_import_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: livejournal-import bundles no Claude skill — a blog-export importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 0
  label: >-
    UNSCORABLE — swamp extension quality errors on livejournal_import.ts's
    broken model upgrade chain (version 2026.07.16.2 vs upgrades[] ending at
    2026.03.29.1: 2026.03.28.1 -> 2026.03.28.2 -> 2026.03.29.1, never reaching
    current); this predates the backfill and the source is byte-frozen here
    so it cannot be repaired in this change. score_ratchet.ts reports an
    unscorable extension as SKIPPED, not a CI failure. Tracked in
    livejournal-import-latent-bugs; do not attempt to fix the chain here.
