# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-victoriametrics,
# wave-2b full build of the Phase D extension-quality backfill).
# Every suite drives model.methods.<m>.execute() against a stubbed fetch
# (+ FakeTime for every start/end/uptime/timestamp) and a fake context.
#
# 2026.08.02.1 fixes all 11 latent bugs found during the original grounding
# pass (VM1-VM11: single-series collapse [VM2, HIGH], no expected-target
# tracking [VM1], absent-metric silent zeros [VM3], boot-time epoch fallback
# [VM4], scalar-query garbage-mapping [VM5], uninspected status:error
# responses [VM6+VM11], query-range vector-body crash [VM7], unguarded inner
# .values/.value accesses [VM8], disk NaN/-Infinity [VM9, folds into VM8],
# and negative-topN end-slice [VM10]) — tracked by the local
# victoriametrics-latent-bugs issue-lifecycle model (never filed to the
# swamp.club Lab). victoriametrics_adversarial_test.ts flips those pins to
# assert the CORRECTED behavior instead of merely characterizing the bug —
# see CHANGELOG.md for the full per-bug breakdown.
schemaVersion: 1
extension: victoriametrics
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/victoriametrics_test.ts
  methods:
    state: present
    files:
      - extensions/models/victoriametrics_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/victoriametrics_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/victoriametrics_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/victoriametrics_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: victoriametrics bundles no Claude skill — a bare PromQL query/overview model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: >-
    Grade A — re-stamped for 2026.09.02.1, which restores the `push` method and
    its additive `pushResult` resource (identity upgrades[] entry, no existing
    resource schema change) and adds 15 tests across the methods, adversarial,
    coverage and property suites. `swamp extension quality manifest.yaml --json`
    emits a real score: 12/12 locally earned at 100%, rubricVersion 3, allPassed
    true, dependencyTrust passed (no runtime dependency besides zod, which the
    dependency-trust factor excludes); repository-verified is provisional
    locally and the server confirms it on publish for the full 14/14. Stamped
    directly from that tool output, not guessed — unchanged in substance from
    the 2026.08.02.1 stamp since this release adds no new dependency.
