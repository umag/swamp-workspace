# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-bandcamp
# (wave-3 scraper child of ext-quality-test-backfill). bandcamp.ts is
# BYTE-FROZEN by this change — every listed test file characterizes/pins
# already-shipped behavior (including 7 latent bugs tracked in the LOCAL
# bandcamp-latent-bugs issue-lifecycle model, never filed to the Lab); no
# production code changed and no manifest/model version bump.
#
# Role map:
#   contract-fixture        -> bandcamp_test.ts
#   methods                 -> bandcamp_methods_test.ts
#   adversarial             -> bandcamp_adversarial_test.ts
#   coverage                -> bandcamp_coverage_test.ts
#   property-invariant-flow -> bandcamp_property_test.ts
schemaVersion: 1
extension: bandcamp
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/bandcamp_test.ts
  methods:
    state: present
    files:
      - extensions/models/bandcamp_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/bandcamp_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/bandcamp_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/bandcamp_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: bandcamp bundles no Claude skill — a scraper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
