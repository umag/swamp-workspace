# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-fragrantica
# (wave-3 scrapers child of ext-quality-test-backfill), then real-fixed by
# the fix/fragrantica-latent-bugs change (LB4–LB12 closed; LB10's
# false-positive selector and LB12's verbatim-storage decision stay
# documented, accepted residuals — see CHANGELOG.md's 2026.08.02.1 entry).
# fragrantica.ts and the model/manifest version WERE bumped by this change.
#
# Role map (each pre-existing file assigned to exactly ONE honest role — the
# scaffolder originally dumped fragrantica_test.ts under contract-fixture,
# which is correct and unchanged; this adds the four missing suites):
#   contract-fixture   -> fragrantica_test.ts (pre-existing, unchanged),
#                         fragrantica_contract_test.ts (new: fixture-corpus
#                         pins of the 4 exported pures + a mechanical
#                         host/secret scan over fixtures/)
#   methods            -> fragrantica_methods_test.ts (new)
#   adversarial        -> fragrantica_adversarial_test.ts (new)
#   coverage           -> fragrantica_coverage_test.ts (new)
#   property-invariant-flow -> fragrantica_property_test.ts (new)
schemaVersion: 1
extension: fragrantica
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/fragrantica_test.ts
      - extensions/models/fragrantica_contract_test.ts
  methods:
    state: present
    files:
      - extensions/models/fragrantica_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/fragrantica_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/fragrantica_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/fragrantica_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: fragrantica bundles no Claude skill — a scraper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
