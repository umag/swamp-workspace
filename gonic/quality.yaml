# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-gonic, wave 2c of
# the Phase D extension-quality backfill). gonic.ts is no longer wholly
# byte-frozen: 2026.08.02.1 real-fixed LB3/LB4/LB5/LB7/LB8/LB9 (the local
# gonic-latent-bugs issue-lifecycle model's remaining open findings) — every
# suite drives model.methods.<m>.execute() against a stubbed fetch + stubbed
# Deno.Command + fake context, now verifying the fixes where flipped and
# characterizing unrelated already-shipped behavior elsewhere.
schemaVersion: 1
extension: gonic
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/gonic_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/gonic_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/gonic_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/gonic_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/gonic_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: gonic bundles no Claude skill — a Subsonic/SSH model client, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
