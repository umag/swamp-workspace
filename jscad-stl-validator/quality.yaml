# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-jscad-stl-validator,
# wave-4 batch-4c extension-quality backfill). jscad_stl_validator.ts and
# jscad/stl_validator.ts are unmodified — every suite drives
# model.methods.<m>.execute() / StlValidator.validate() against synthetic
# fixtures, characterizing already-shipped behavior.
schemaVersion: 1
extension: jscad-stl-validator
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/jscad/stl_validator_test.ts
  methods:
    state: present
    files:
      - extensions/models/jscad_stl_validator_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/jscad_stl_validator_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/jscad_stl_validator_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/jscad_stl_validator_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: jscad-stl-validator bundles no Claude skill — a geometry validator, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
