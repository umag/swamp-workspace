# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-victorialogs, wave
# 2b of the Phase D extension-quality backfill). victorialogs.ts is
# unmodified — every suite drives model.methods.<m>.execute() (and, in the
# contract-fixture suite, model.resources.<r>.schema) against a stubbed
# fetch + stubbed Deno.Command + fake context, characterizing already-shipped
# behavior.
schemaVersion: 1
extension: victorialogs
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/victorialogs_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/victorialogs_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/victorialogs_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/victorialogs_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/victorialogs_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: victorialogs bundles no Claude skill — a bare LogsQL query model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
