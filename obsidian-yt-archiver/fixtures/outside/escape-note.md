# Fixture Escape Target (outside the vault subtree)

This file lives OUTSIDE fixtures/vault and must only be reachable via the
unsanitized `folder` traversal bug (LB2).

https://www.youtube.com/watch?v=fixtureOUT6
