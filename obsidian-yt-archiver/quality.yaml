# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-obsidian-yt-archiver,
# wave-4 batch-4b of the extension-quality backfill). 2026.08.02.1 fixed all
# seven remaining latent bugs (LB1, LB3-LB8; LB2 fixed earlier) in
# obsidian_yt_archiver.ts — every suite still drives
# model.methods.<m>.execute() against a REAL temporary vault directory + a
# stubbed fetch, now characterizing the hardened, fixed behavior.
schemaVersion: 1
extension: obsidian-yt-archiver
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/obsidian_yt_archiver_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/obsidian_yt_archiver_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/obsidian_yt_archiver_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/obsidian_yt_archiver_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/obsidian_yt_archiver_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: obsidian-yt-archiver bundles no Claude skill — a read-only Obsidian-vault → TubeArchivist archival wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
