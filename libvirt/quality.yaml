# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-libvirt (wave 1
# gap-check child of ext-quality-test-backfill, per the pihole PR #66
# recipe). libvirt is REMOVED from quality-allowlist.txt in the same change
# as these flips.
schemaVersion: 1
extension: libvirt
tests:
  # contract-fixture is anchored by the three PRE-EXISTING suites, kept
  # byte-unchanged as the anchor: libvirt_parse_test.ts (pure virsh
  # table/XML parsers), libvirt_connection_test.ts (buildInvocation argv +
  # shellQuote command-injection + redactSecrets + hardened SSH opts), and
  # libvirt_idempotency_test.ts (IDEMPOTENT_ERRORS needle sets vs real virsh
  # 8.7 strings). Role assigned by-convenience — connection_test also carries
  # substantial adversarial content and idempotency_test also carries
  # coverage content; the four new suites below complement, not duplicate,
  # them (see each new suite's own header comment for its distinct scope).
  contract-fixture:
    state: present
    files:
      - extensions/models/libvirt_parse_test.ts
      - extensions/models/libvirt_connection_test.ts
      - extensions/models/libvirt_idempotency_test.ts
  methods:
    state: present
    files:
      - extensions/models/libvirt_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/libvirt_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/libvirt_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/libvirt_property_test.ts
watch:
  state: backlog
  justification: no generalized release-watch wired yet — tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: live canary workflow not built yet — tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: this extension bundles no Claude skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
