# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-observability-agent,
# wave 2c of the Phase D extension-quality backfill). observability_agent.ts
# is unmodified — every suite drives model.methods.<m>.execute() (and, in the
# contract-fixture suite, model.resources.<spec>.schema) against a stubbed
# Deno.Command (SSH boundary) + fake context, characterizing already-shipped
# behavior, including several found bugs (see CHANGELOG.md and the LOCAL
# observability-agent-rce issue-lifecycle bug model — never the Lab).
schemaVersion: 1
extension: observability-agent
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/observability_agent_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/observability_agent_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/observability_agent_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/observability_agent_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/observability_agent_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: observability-agent bundles no Claude skill — a bare SSH installer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 85
  label: "honest 85% (12/14, rubricV3) — CLI exits 1 per Lab #1481 (rich-readme + symbols-docs gaps); score_ratchet SKIPS, does not fail"
