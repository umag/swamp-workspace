# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-talos-node, wave-1
# full build of the Phase D extension-quality backfill). talos.ts is
# unmodified — every suite drives model.methods.<m>.execute() against a
# stubbed Deno.Command + fake context, characterizing already-shipped
# behavior.
schemaVersion: 1
extension: talos-node
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/talos_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/talos_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/talos_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/talos_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/talos_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: talos-node bundles no Claude skill — a bare talosctl CLI wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
