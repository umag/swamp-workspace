# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-shoko, wave-2a of
# the Phase D extension-quality backfill). shoko.ts is unmodified — every
# suite drives model.methods.<m>.execute() against a stubbed fetch + fake
# context, characterizing already-shipped behavior.
schemaVersion: 1
extension: shoko
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/shoko_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/shoko_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/shoko_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/shoko_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/shoko_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: shoko bundles no Claude skill — a bare REST v3 read/action wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
