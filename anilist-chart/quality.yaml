# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-anilist-chart,
# wave-4 batch-4d, FINAL batch of the Phase D extension-quality backfill).
# At that time anilist_chart.ts and every lib/*.ts file were BYTE-FROZEN — no
# production code changed, no manifest/model version bump.
#
# 2026.08.01.1 (anilist-chart-grade-a, LOCAL issue-lifecycle model): the
# entrypoint freeze above ended — added real JSDoc to the two exported
# symbols that were missing it (RenderTaskBundle, model), earning the last
# rubric point (symbols-docs, 50% -> 100%). Docs-only: no runtime behavior
# changed, no test flips. Ratchet moves 92% -> 100% (14/14) = Grade A.
#
# 2026.08.02.1 (anilist-chart-latent-bugs, LOCAL issue-lifecycle model):
# real-fixed all 7 latent bugs below (LB1-LB7) in anilist_chart.ts and
# lib/clickhouse.ts; two DEFAULTED global args added (sshTimeoutMs,
# clickhouseMaxResponseBytes) via a single identity upgrades[] entry. All
# five suites stay present; only the pinned characterization of each fixed
# bug flipped from "pins the bug" to "pins the fix" (see CHANGELOG.md for
# the exact flip list), plus one additive unit in render_run.test.ts. The
# contract-fixture, property-invariant-flow, and happy-path methods suites
# are byte-identical. Re-measured via `swamp extension quality
# manifest.yaml --repo-dir <workspace-root> --json`: ratchet unchanged at
# 100% (14/14, rubricVersion 3) — Grade A.
#
# Role map (the scaffolder originally dumped all 16 pre-existing test files
# under contract-fixture; this corrects that to their HONEST primary role —
# a suite label denotes a file's DOMINANT role, not its exclusive content,
# since `deno test` runs every listed file regardless of label):
#   contract-fixture (12, external-oracle/byte/schema/render parity):
#     lib/bayesian.test.ts, lib/age_penalty.test.ts, lib/format.test.ts,
#     lib/chart_rank.test.ts, lib/awards.test.ts, lib/pairs.test.ts,
#     lib/render_board.test.ts, lib/render_charts.test.ts,
#     lib/render_landing.test.ts, lib/css_parity.test.ts (byte-parity,
#     test:css), lib/clickhouse.test.ts (live schema contract, test:live),
#     anilist_chart.test.ts (7-artifact fan-out + model surface)
#   coverage (4, port-invented internal guard/gate/fault-isolation branches
#     — no external oracle): lib/rankable.test.ts, lib/render_run.test.ts
#     (ALSO carries oracle-parity assertions — freshness-gate matrix +
#     fan-out fault-isolation is its dominant role), lib/publish.test.ts,
#     lib/publish_gate.test.ts
#   methods (NEW anilist_chart_methods_test.ts): drives
#     model.methods.settings/render/publish .execute() against a stubbed
#     globalThis.fetch (ClickHouse HTTP) + fake context + FakeTime; publish
#     exercised via the LOCAL Deno.makeTempDir path (no Deno.Command stub —
#     Deno-skew rule forbids an `as typeof Deno.Command` cast; the ssh
#     branch's fault semantics are covered by lib/publish.test.ts's
#     injected-writer unit, agnostic to which writer is plugged in)
#   adversarial (NEW anilist_chart_adversarial_test.ts): hostile ClickHouse
#     HTTP responses, HTML/CSS injection through execute(), and the two
#     latent bugs (LB2, LB7) that resist stubbing through the model
#     boundary (pinned via pure-function / source-text tests instead)
#   property-invariant-flow (NEW anilist_chart_property_test.ts):
#     npm:fast-check@4.8.0 (pinned exact version), FC_NUM_RUNS-gated,
#     restricted arbitraries — bayesian/ranking/age-penalty/esc invariants
#     plus one end-to-end fan-out FLOW property
#
# 7 LOW/MEDIUM latent bugs (0 CRITICAL/HIGH), tracked in the LOCAL
# `anilist-chart-latent-bugs` issue-lifecycle model — never the
# swamp.club Lab (this extension is @magistr/*, not @swamp/*) — are ALL
# FIXED as of 2026.08.02.1 (see the header note above and CHANGELOG.md).
schemaVersion: 1
extension: anilist-chart
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/lib/bayesian.test.ts
      - extensions/models/lib/age_penalty.test.ts
      - extensions/models/lib/format.test.ts
      - extensions/models/lib/chart_rank.test.ts
      - extensions/models/lib/awards.test.ts
      - extensions/models/lib/pairs.test.ts
      - extensions/models/lib/render_board.test.ts
      - extensions/models/lib/render_charts.test.ts
      - extensions/models/lib/render_landing.test.ts
      - extensions/models/lib/css_parity.test.ts
      - extensions/models/lib/clickhouse.test.ts
      - extensions/models/anilist_chart.test.ts
  methods:
    state: present
    files:
      - extensions/models/anilist_chart_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/anilist_chart_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/lib/rankable.test.ts
      - extensions/models/lib/render_run.test.ts
      - extensions/models/lib/publish.test.ts
      - extensions/models/lib/publish_gate.test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/anilist_chart_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: anilist-chart bundles no Claude skill — a chart-rendering/publish pipeline, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: >-
    100% (14/14, rubricVersion 3) — Grade A. Five suites present, docs
    present, rich-readme earned by expanding README.md, and symbols-docs
    earned: JSDoc on the two exported symbols that were missing it
    (RenderTaskBundle, model), bringing entrypoint coverage to 4/4 (100%,
    >=80% threshold). Re-measured after the 2026.08.02.1 real-fix of all 7
    latent bugs (LB1-LB7): status "passed", allPassed true, earnedPoints
    14/14, unchanged from the prior stamp. Measured via
    `swamp extension quality manifest.yaml --repo-dir <workspace-root> --json`
    (stdout is valid despite a non-zero exit — Lab #1481).
