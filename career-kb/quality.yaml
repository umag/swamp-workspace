# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-career-kb
# (wave-4 batch-4b child of the extension-quality backfill program,
# ext-quality-test-backfill). career_kb.ts and manifest.yaml are BYTE-FROZEN
# by this change — every listed test file characterizes/pins already-shipped
# behavior; no production code changed and no manifest/model version bump.
#
# Role map (each file assigned to exactly ONE honest role):
#   contract-fixture        -> career_kb_test.ts (unchanged — pins behavior
#                               against the REAL bundled references/ corpus)
#   methods                 -> career_kb_methods_test.ts (new — synthetic
#                               fixtures/references/ corpus)
#   adversarial              -> career_kb_adversarial_test.ts (new)
#   coverage                 -> career_kb_coverage_test.ts (new)
#   property-invariant-flow -> career_kb_property_test.ts (new)
schemaVersion: 1
extension: career-kb
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/career_kb_test.ts
  methods:
    state: present
    files:
      - extensions/models/career_kb_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/career_kb_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/career_kb_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/career_kb_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/career/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
