# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-comfyui (wave 2c
# gap-check child of ext-quality-test-backfill); comfyui.ts was byte-frozen
# at that point (2026.07.21.1).
#
# 2026.08.02.1 (fix/comfyui-latent-bugs) real-fixes the four latent bugs that
# release's adversarial suite characterized: `comfyui.ts` (unapplied-seed
# record, `snapshotServer` res.ok check, `saveImages` subfolder join) and
# `lib/comfy_client.ts` (`waitForResult` errored-empty throw) both changed.
# The four former pins in `comfyui_adversarial_test.ts` now assert the
# CORRECTED behavior instead of the buggy one; the `tests:` file lists below
# are unchanged (all edits were made in-place, no files added/removed).
#
# Role map (each pre-existing file assigned to exactly ONE honest role — the
# scaffolder originally dumped all 8 under contract-fixture; this corrects
# that):
#   methods            -> base.test.ts, comfyui.test.ts, comfyui_methods_test.ts
#   contract-fixture   -> lib/comfy_client.test.ts, lib/anthropic.test.ts,
#                         lib/caption.test.ts, lib/caption_generated.test.ts,
#                         lib/ideogram_prompt.test.ts, lib/workflow_patch.test.ts,
#                         comfyui_contract_test.ts (new: bundled-asset pin)
#   adversarial        -> comfyui_adversarial_test.ts (new)
#   coverage           -> comfyui_coverage_test.ts (new)
#   property-invariant-flow -> comfyui_property_test.ts (new)
schemaVersion: 1
extension: comfyui
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/lib/comfy_client.test.ts
      - extensions/models/lib/anthropic.test.ts
      - extensions/models/lib/caption.test.ts
      - extensions/models/lib/caption_generated.test.ts
      - extensions/models/lib/ideogram_prompt.test.ts
      - extensions/models/lib/workflow_patch.test.ts
      - extensions/models/comfyui_contract_test.ts
  methods:
    state: present
    files:
      - extensions/models/base.test.ts
      - extensions/models/comfyui.test.ts
      - extensions/models/comfyui_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/comfyui_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/comfyui_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/comfyui_property_test.ts
watch:
  state: backlog
  justification: no generalized release-watch yet — tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: live canary workflow not built yet — tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/comfyui/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
