{
  "tasks": {
    "test": "deno test --allow-read --allow-write --allow-env=FC_NUM_RUNS extensions/models/",
    "test:soak": "FC_NUM_RUNS=10000 deno test --allow-read --allow-write --allow-env=FC_NUM_RUNS extensions/models/comfyui_property_test.ts",
    "check": "deno check extensions/models/*.ts extensions/models/lib/*.ts",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint"
  },
  "fmt": {
    "lineWidth": 80
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["require-await"]
    },
    "exclude": [".jj"]
  }
}
