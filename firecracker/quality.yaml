# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-firecracker,
# wave-4 batch-4c of the Phase D extension-quality backfill). firecracker.ts
# and lib/ssh.ts are unmodified — every suite drives model.methods.<m>.execute()
# (and, in the contract-fixture suite, the exported pure builders) against a
# stubbed Deno.Command (SSH boundary) + fake context, characterizing
# already-shipped behavior, including six found bugs (see CHANGELOG.md and
# the LOCAL firecracker-latent-bugs issue-lifecycle bug model — never the
# Lab).
schemaVersion: 1
extension: firecracker
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/firecracker_test.ts
      - extensions/models/firecracker_outputformat.test.ts
      - extensions/models/firecracker.test.ts
  methods:
    state: present
    files:
      - extensions/models/firecracker_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/firecracker_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/firecracker_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/firecracker_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: firecracker bundles no Claude skill — a microVM manager over SSH, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
