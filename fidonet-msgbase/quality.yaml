# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-fidonet-msgbase,
# wave-4 batch-4a of the Phase D extension-quality backfill), then real-fixed
# for all 9 latent bugs the backfill's adversarial/coverage suites found
# (fix/fidonet-msgbase-latent-bugs, 2026.08.02.1) — every suite drives
# model.methods.<m>.execute() against a fake context (globalArgs +
# writeResource + readResource) with basePath pointing at a synthetic
# fixture tree written by fixtures/builders.ts; real Deno.readFile/readDir
# over byte-accurate hand-authored bytes, no FS stubbing.
#
# Role map (one file per suite, no scaffolder-dumped ambiguity):
#   methods                 -> fidonet_msgbase_methods_test.ts
#   contract-fixture         -> fidonet_msgbase_contract_test.ts
#   adversarial              -> fidonet_msgbase_adversarial_test.ts
#   coverage                 -> fidonet_msgbase_coverage_test.ts
#   property-invariant-flow -> fidonet_msgbase_property_test.ts
schemaVersion: 1
extension: fidonet-msgbase
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/fidonet_msgbase_contract_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/fidonet_msgbase_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/fidonet_msgbase_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/fidonet_msgbase_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/fidonet_msgbase_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: fidonet-msgbase bundles no Claude skill — a filesystem importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
