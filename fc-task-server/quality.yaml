# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-fc-task-server,
# wave-4 batch-4b of the Phase D extension-quality backfill). fc_task_server.ts
# and lib/ssh.ts are unmodified — every suite drives model.methods.<m>.execute()
# (and, in the contract-fixture suite, model.resources.<spec>.schema) against a
# stubbed Deno.Command (SSH boundary) + fake context, characterizing
# already-shipped behavior, including several found bugs (see CHANGELOG.md and
# the LOCAL fc-task-server-latent-bugs issue-lifecycle bug model — never the
# Lab).
schemaVersion: 1
extension: fc-task-server
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/fc_task_server_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/fc_task_server_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/fc_task_server_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/fc_task_server_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/fc_task_server_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: fc-task-server bundles no Claude skill — a bare host↔guest control-plane deployer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
