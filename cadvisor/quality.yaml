# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-cadvisor, wave 2c
# full build of the Phase D extension-quality backfill); every suite drives
# model.methods.<m>.execute() against BOTH a stubbed Deno.Command (SSH) and a
# stubbed fetch (HTTP). As of 2026.08.02.1, all six remaining
# cadvisor-latent-bugs are fixed and every suite is re-baselined to the fixed
# behavior — see CHANGELOG.md.
schemaVersion: 1
extension: cadvisor
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/cadvisor_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/cadvisor_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/cadvisor_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/cadvisor_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/cadvisor_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: cadvisor bundles no Claude skill — a bare SSH+HTTP metrics model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
