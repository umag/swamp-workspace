# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-headphones,
# wave-2b full build of the Phase D extension-quality backfill). Every suite
# drives model.methods.<m>.execute() against a stubbed fetch (+ a stubbed
# Deno.Command for audit-library) and a fake context. The apikey-in-URL-query
# fetch-rejection leak and the get-artist/get-album array non-unwrap were
# later fixed via the local `headphones-apikey-hardening` issue-lifecycle
# model (redactSecrets wrapper + array unwrap, version 2026.08.01.1) — see
# CHANGELOG.md.
schemaVersion: 1
extension: headphones
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/headphones_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/headphones_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/headphones_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/headphones_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/headphones_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: headphones bundles no Claude skill — a bare music-automation API wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
