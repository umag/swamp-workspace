# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-music-library,
# wave-4 batch-4b of the Phase D extension-quality backfill). music_library.ts
# is BYTE-FROZEN — every suite drives model.methods.<m>.execute() against a
# stubbed Deno.Command (the sshRun ssh subprocess) + fake context,
# characterizing already-shipped behavior.
#
# Role map (each test file assigned to exactly ONE honest role, per
# STANDARD.md's "named by ROLE, never by filename convention"):
#   contract-fixture       -> music_library_test.ts +
#                              extensions/reports/verify_triage_test.ts
#   methods                 -> music_library_methods_test.ts
#   adversarial              -> music_library_adversarial_test.ts
#   coverage                 -> music_library_coverage_test.ts
#   property-invariant-flow -> music_library_property_test.ts
schemaVersion: 1
extension: music-library
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/music_library_test.ts"
      - "extensions/reports/verify_triage_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/music_library_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/music_library_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/music_library_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/music_library_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: music-library bundles no Claude skill — a gonic-index catalog model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
