# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-arckit
# (wave-4 batch-4d, FINAL batch of the extension-quality backfill program,
# ext-quality-test-backfill). arckit_workspace.ts and manifest.yaml were
# BYTE-FROZEN by that backfill — every listed test file characterized/pinned
# already-shipped behavior at the time; no production code changed and no
# manifest/model version bump.
#
# 2026.08.01.1: fixed the HIGH path-traversal latent bug (LB1, tracked in the
# LOCAL `arckit-latent-bugs` issue-lifecycle model — never a swamp.club Lab
# issue) — startProject.execute now rejects any `dir` that is not a single
# NNN-slug segment before readProjectState/Deno.mkdir/writeResource run. This
# is the first production change to arckit_workspace.ts since the backfill;
# it is no longer byte-frozen. manifest.yaml and model.version bumped in
# lockstep. The two LB1 pins in the adversarial suite are now fix-regression
# tests (rejection), not current-behavior pins; LB2..LB7 remain deferred
# pins, untouched.
#
# Role map (each file assigned to exactly ONE honest role):
#   contract-fixture        -> arckit_workspace_test.ts (unchanged — pins
#                               behavior against the REAL bundled templates/
#                               corpus + pure reference-table logic)
#   methods                 -> arckit_workspace_methods_test.ts (new — all 12
#                               methods, synthetic Deno.makeTempDir workspace)
#   adversarial              -> arckit_workspace_adversarial_test.ts (new —
#                               hostile inputs + LB1..LB7 pins)
#   coverage                 -> arckit_workspace_coverage_test.ts (new —
#                               profile-extra gates, terminal states, LB4/LB5/
#                               LB6 reinforcement)
#   property-invariant-flow -> arckit_workspace_property_test.ts (new —
#                               fast-check@4.8.0, restricted arbitraries)
schemaVersion: 1
extension: arckit
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/arckit_workspace_test.ts
  methods:
    state: present
    files:
      - extensions/models/arckit_workspace_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/arckit_workspace_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/arckit_workspace_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/arckit_workspace_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/arckit/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
