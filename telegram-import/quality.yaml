# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-telegram-import,
# wave-4 batch-4a child of the extension-quality backfill program). telegram_
# import.ts is unmodified — every suite drives model.methods.import.execute()
# against a fake ctx + a full Deno.Command/copyFile/mkdir/makeTempDir/remove
# stub seam, characterizing already-shipped behavior.
#
# Role map (each new file assigned exactly ONE role — there was no
# pre-existing test file to gap-check, so this is a from-scratch build, not
# a reclassification):
#   contract-fixture        -> telegram_import_contract_test.ts
#   methods                 -> telegram_import_methods_test.ts
#   adversarial              -> telegram_import_adversarial_test.ts
#   coverage                 -> telegram_import_coverage_test.ts
#   property-invariant-flow -> telegram_import_property_test.ts
schemaVersion: 1
extension: telegram-import
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/telegram_import_contract_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/telegram_import_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/telegram_import_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/telegram_import_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/telegram_import_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: telegram-import bundles no Claude skill — an export importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 0
  label: >-
    UNSCORABLE — swamp extension quality errors on telegram_import.ts's
    broken model upgrade chain (declared version 2026.07.16.2, but the only
    declared upgrade entry ends at toVersion 2026.03.28.2); this predates
    the backfill and telegram_import.ts is byte-frozen here so it cannot be
    repaired in this change. score_ratchet.ts reports an unscorable
    extension as SKIPPED, not a CI failure. Tracked in
    telegram-import-latent-bugs (LB-0); do not attempt to fix the chain
    here.
