# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# HISTORICAL NOTE (as of the ext-quality-bf-telegram-import backfill):
# backfilled to full five-suite compliance (wave-4 batch-4a child of the
# extension-quality backfill program). telegram_import.ts was UNMODIFIED at
# that time — every suite drove model.methods.import.execute() against a fake
# ctx + a full Deno.Command/copyFile/mkdir/makeTempDir/remove stub seam,
# characterizing already-shipped behavior; no production code changed and no
# manifest/model version bump in that backfill.
#
# THE FREEZE HAS SINCE BROKEN: 2026.08.01.1 (see CHANGELOG.md and the
# `ratchet` block below) fixes latent bug LB-1 (HIGH path-traversal) with a
# posix-normalize path-containment guard (isPathContained/safeCopyMedia) at
# the photo/file/video Deno.copyFile sites, repairs the model upgrade chain
# (LB-0), and bumps model.version/manifest.yaml. Production code DID change in
# that release — this header's "unmodified" language above describes the
# ORIGINAL backfill only, not the current state of telegram_import.ts.
#
# 2026.08.01.2 (swamp-workspace #57, mirrors PR #56's obsidian-vault backend
# split) adds an optional headless vaultRoot filesystem backend to `import`
# and upgrades isPathContained/safeCopyMedia from lexical-only to
# realpath-aware, closing the documented symlink-escape residual. All five
# suites extended with vaultRoot coverage — see CHANGELOG.md.
#
# 2026.08.02.1 real-fixes all eight remaining latent bugs (LB-2..LB-9,
# telegram-import-latent-bugs) — note-slug traversal guard, YAML frontmatter
# escaping, per-message error isolation, export shape validation, a
# bounded+checked find subprocess, code-point-safe text truncation, a
# result.json size guard, and leading-dash zipPath normalization. Also adds a
# JSDoc comment to the (pre-existing, undocumented) VaultPath interface,
# restoring the symbols-docs ratchet factor to earned. See CHANGELOG.md.
#
# Role map (each new file assigned exactly ONE role — there was no
# pre-existing test file to gap-check, so this is a from-scratch build, not
# a reclassification):
#   contract-fixture        -> telegram_import_contract_test.ts
#   methods                 -> telegram_import_methods_test.ts
#   adversarial              -> telegram_import_adversarial_test.ts
#   coverage                 -> telegram_import_coverage_test.ts
#   property-invariant-flow -> telegram_import_property_test.ts
schemaVersion: 1
extension: telegram-import
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/telegram_import_contract_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/telegram_import_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/telegram_import_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/telegram_import_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/telegram_import_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: telegram-import bundles no Claude skill — an export importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: >-
    Grade A — the model upgrade chain is repaired and continuous
    (2026.03.28.1 -> 2026.03.28.2 -> 2026.05.25.1 -> 2026.07.16.2 ->
    2026.08.01.1 -> 2026.08.01.2 -> 2026.08.02.1, final toVersion ===
    model.version) across the LB-1 path-traversal fix (2026.08.01.1), the
    swamp-workspace #57 vaultRoot/LB-1-residual change (2026.08.01.2), and the
    LB-2..LB-9 real-fix release (2026.08.02.1). `swamp extension quality
    manifest.yaml --json` now emits a real score: 14/14 points (100%),
    allPassed true, dependencyTrust passed (jsr:@std/path audited clean).
    Stamped directly from that tool output, not guessed.
