# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
schemaVersion: 1
extension: swamp-go-brr
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/source_integration_framing.test.ts
      - extensions/models/otlp_export.test.ts
      - extensions/models/docker_verify.test.ts
      - extensions/models/source_integration_leaf_json.test.ts
      - extensions/models/source_integration_applied_result.test.ts
      - extensions/models/gobrr.test.ts
      - extensions/models/preflight.test.ts
      - extensions/models/lib/otlp.test.ts
      - extensions/models/lib/acl.test.ts
      - extensions/models/lib/scrub.test.ts
      - extensions/models/gobrr_observability.test.ts
      - extensions/models/source_integration.test.ts
  methods:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
  adversarial:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
  coverage:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
  property-invariant-flow:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
  changelog:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
  skill:
    state: backlog
    justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
ratchet:
  rubricVersion: 3
  baselinePercentage: 0
  label: unscored
