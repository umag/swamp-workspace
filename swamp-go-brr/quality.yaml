# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# 2026.08.02.1 (swamp-go-brr-latent-bugs): all eight latent bugs (B1..B8),
# previously pinned as characterization tests over a byte-frozen source, are
# now REAL-FIXED — gobrr.ts, docker_verify.ts, otlp_export.ts, preflight.ts,
# source_integration.ts, and lib/{acl,otlp,scrub,ssh}.ts all changed; every
# adversarial pin now asserts the FIXED behavior. All five models bump to
# 2026.08.02.1 (lockstep) with a first upgrades[] entry apiece. Re-stamped
# from a real `swamp extension quality` run (14/14, 100%, allPassed=true).
#
# Role map (the scaffolder originally dumped all 12 pre-existing files under
# contract-fixture; this corrects that to an honest per-file role):
#   contract-fixture -> source_integration.test.ts, source_integration_framing.test.ts,
#                        source_integration_applied_result.test.ts, lib/otlp.test.ts,
#                        gobrr_observability.test.ts (byte-identical — none of
#                        these touch Deno.Command/jjRun/ssh, only pure functions
#                        unaffected by the B1-B8 fixes)
#   methods          -> gobrr.test.ts, otlp_export.test.ts, preflight.test.ts,
#                        extensions/models/swamp_go_brr_methods_test.ts
#   adversarial      -> lib/acl.test.ts, lib/scrub.test.ts, docker_verify.test.ts,
#                        source_integration_leaf_json.test.ts,
#                        extensions/models/swamp_go_brr_adversarial_test.ts
#   coverage         -> extensions/models/swamp_go_brr_coverage_test.ts
#   property-invariant-flow -> extensions/models/swamp_go_brr_property_test.ts
#
# B1..B8 fixed: B1 (MED) docker-verify's ssh transport now enforces a
# client-side timeout (verifyTimeoutMs); B2 (MED, within-temp)
# preflight.scaffold now pre-validates every ScaffoldFile.path with
# pathEscapes before any write; B3 (MED) lib/ssh.ts now defaults to
# StrictHostKeyChecking=accept-new (documented "no" opt-out); B4 (LOW)
# lib/scrub.ts's generic key=value floor raised {8,}->{11,} and scrubSecrets
# now enforces its own tail-preserving MAX_SCRUB_BYTES cap; B5 (LOW) a bare
# high-entropy run (>=32 chars, 3 char-classes) is now redacted with no key
# word required; B6 (LOW) source-integration's jjRun now carries --no-pager +
# a client-side timeout (jjTimeoutMs); B7 (LOW) parseGitDiffPaths now splits
# a diff header symmetrically (no longer mis-splits a literal " b/" in a
# filename) — still unreachable via the real apply() flow either way
# (pathEscapes rejects whitespace upstream); B8 (LOW) apply()'s write loop now
# goes through lib/acl.ts's safeWriteWithinRepo + confineWrittenPath
# (TOCTOU detect-and-clean-up).
schemaVersion: 1
extension: swamp-go-brr
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/source_integration.test.ts
      - extensions/models/source_integration_framing.test.ts
      - extensions/models/source_integration_applied_result.test.ts
      - extensions/models/lib/otlp.test.ts
      - extensions/models/gobrr_observability.test.ts
  methods:
    state: present
    files:
      - extensions/models/gobrr.test.ts
      - extensions/models/otlp_export.test.ts
      - extensions/models/preflight.test.ts
      - extensions/models/swamp_go_brr_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/lib/acl.test.ts
      - extensions/models/lib/scrub.test.ts
      - extensions/models/docker_verify.test.ts
      - extensions/models/source_integration_leaf_json.test.ts
      - extensions/models/swamp_go_brr_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/swamp_go_brr_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/swamp_go_brr_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
# soak: swamp-go-brr's test task is bare `--allow-all` — the broadest
# possible grant — because its suite stubs Deno.Command/jjRun/ssh entirely
# rather than scoping real filesystem/subprocess access; there is no
# narrower real set of paths/binaries to enumerate. Narrowed here to just
# --allow-env=FC_NUM_RUNS (the only thing the property test harness itself
# reads) per scripts/lib/soak_permissions.ts's checkSoakAuthority — an
# unattended nightly soak must never silently inherit --allow-all. Tracked
# in ext-quality-release-watch-soak (Phase B, property-soak-permission-
# source-of-truth).
soak:
  state: present
  denoArgs:
    - "--allow-env=FC_NUM_RUNS"
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/swamp-go-brr/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
