# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-swamp-go-brr,
# wave-4 batch-4d, FINAL batch of the Phase D extension-quality backfill).
# gobrr.ts, docker_verify.ts, otlp_export.ts, preflight.ts, source_integration.ts,
# and lib/{acl,otlp,scrub,ssh}.ts are BYTE-FROZEN by this change — every suite
# listed below either drives model.methods.<m>.execute() against a stubbed
# Deno.Command (BOTH the "direct output" ssh/jjRun shape and preflight's
# "spawn+stdin" shape) + fake context, or exercises an EXPORTED pure
# function/builder directly. No production code changed and no manifest/model
# version bump.
#
# Role map (the scaffolder originally dumped all 12 pre-existing files under
# contract-fixture; this corrects that to an honest per-file role):
#   contract-fixture -> source_integration.test.ts, source_integration_framing.test.ts,
#                        source_integration_applied_result.test.ts, lib/otlp.test.ts,
#                        gobrr_observability.test.ts
#   methods          -> gobrr.test.ts, otlp_export.test.ts, preflight.test.ts,
#                        extensions/models/swamp_go_brr_methods_test.ts (new)
#   adversarial      -> lib/acl.test.ts, lib/scrub.test.ts, docker_verify.test.ts,
#                        source_integration_leaf_json.test.ts,
#                        extensions/models/swamp_go_brr_adversarial_test.ts (new)
#   coverage         -> extensions/models/swamp_go_brr_coverage_test.ts (new)
#   property-invariant-flow -> extensions/models/swamp_go_brr_property_test.ts (new)
#
# Eight known-but-unfixed issues (B1..B8) are pinned as characterization tests
# in the new adversarial suite, tracked in the LOCAL `swamp-go-brr-latent-bugs`
# issue-lifecycle bug model (never the Lab, per this repo's convention):
# B1 (MED) docker-verify has no client-side timeout on the remote verify
# command; B2 (MED, within-temp) preflight.scaffold's ScaffoldFile.path has no
# traversal guard; B3 (MED) lib/ssh.ts disables ssh host-key verification;
# B4 (LOW) lib/scrub.ts's generic key=value pattern is a documented
# false-positive-prone over-approximation with no input-size cap of its own;
# B5 (LOW) a bare high-entropy secret with no recognizable key word is not
# redacted (accepted, documented gap); B6 (LOW) source-integration's jjRun has
# no timeout/--no-pager; B7 (LOW) parseGitDiffPaths mis-splits a path
# containing a literal " b/" substring, but this is UNREACHABLE via the real
# apply() flow (pathEscapes rejects whitespace upstream); B8 (LOW) apply()'s
# resolve-then-write is not atomic (TOCTOU symlink-swap).
schemaVersion: 1
extension: swamp-go-brr
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/source_integration.test.ts
      - extensions/models/source_integration_framing.test.ts
      - extensions/models/source_integration_applied_result.test.ts
      - extensions/models/lib/otlp.test.ts
      - extensions/models/gobrr_observability.test.ts
  methods:
    state: present
    files:
      - extensions/models/gobrr.test.ts
      - extensions/models/otlp_export.test.ts
      - extensions/models/preflight.test.ts
      - extensions/models/swamp_go_brr_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/lib/acl.test.ts
      - extensions/models/lib/scrub.test.ts
      - extensions/models/docker_verify.test.ts
      - extensions/models/source_integration_leaf_json.test.ts
      - extensions/models/swamp_go_brr_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/swamp_go_brr_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/swamp_go_brr_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/swamp-go-brr/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
