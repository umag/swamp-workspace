# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-tubearchivist,
# wave-2a of the Phase D extension-quality backfill). tubearchivist.ts is
# unmodified — every suite drives model.methods.<m>.execute() against a
# stubbed fetch + fake context, characterizing already-shipped behavior.
schemaVersion: 1
extension: tubearchivist
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/tubearchivist_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/tubearchivist_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/tubearchivist_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/tubearchivist_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/tubearchivist_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: tubearchivist bundles no Claude skill — a REST API wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
