# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
schemaVersion: 1
extension: swamp-mongodb-datastore
tests:
  contract-fixture:
    state: present
    files:
      - extensions/datastores/mongodb/config_test.ts
      - extensions/datastores/mongodb/sidecar_test.ts
  methods:
    # Was `na` up to 2026.09.01.1 on the grounds that this package ships a
    # datastore provider and declares no model. As of 2026.09.01.2 it also
    # ships the @magistr/mongodb-datastore-maintenance model, so the suite is
    # now genuinely applicable and present.
    state: present
    files:
      - extensions/models/maintenance_test.ts
  adversarial:
    state: present
    files:
      - extensions/datastores/mongodb/sync_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/datastores/mongodb/sync_coverage_test.ts
      - extensions/models/sweeps_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/sync_property_test.ts
soak:
  state: present
  denoArgs: ["--allow-read", "--allow-write", "--allow-env=FC_NUM_RUNS"]
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: datastore extension — bundles no Claude skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
