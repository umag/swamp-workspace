# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-flipper-zero,
# wave-4 batch-4c of the extension-quality backfill). flipper_zero.ts,
# lib/serial.ts, lib/rpc.ts, lib/protocol.ts, lib/image.ts and
# bots/snake_bot.ts are all unmodified — every suite drives
# model.methods.<m>.execute() against a dual-shape Deno.Command stub + fake
# context, characterizing already-shipped behavior. Seven latent bugs
# found while characterizing are pinned by the adversarial suite and tracked
# in the LOCAL @magistr/issue-lifecycle model flipper-zero-latent-bugs
# (never the Lab).
schemaVersion: 1
extension: flipper-zero
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/flipper_zero_test.ts
      - extensions/models/lib/image_test.ts
  methods:
    state: present
    files:
      - extensions/models/flipper_zero_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/flipper_zero_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/flipper_zero_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/flipper_zero_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: flipper-zero bundles no Claude skill — a USB-serial device controller, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
