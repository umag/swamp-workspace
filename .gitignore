# Swamp runtime data (per-extension, never commit)
**/.swamp/
.swamp-sources.yaml

# Claude Code local-only settings
**/.claude/settings.local.json
**/.claude/projects/

# Generated promptfoo tests
promptfoo.generated.yaml

# Tessl project-local identity + init cruft (eval scenarios + tile.json ARE tracked)
**/tessl.json
**/.tessl/
# MCP/agent configs auto-written by `tessl init` (regenerated on demand)
**/.mcp.json
**/.cursor/
**/.gemini/
**/.codex/
**/.vscode/

# OS
.DS_Store
