# Swamp runtime data (never commit live model state)
.swamp/
.swamp-sources.yaml

# Claude Code local-only settings (keep skills tracked)
.claude/settings.local.json
.claude/projects/

# Generated promptfoo tests — rebuilt from per-skill trigger_evals.json
promptfoo.generated.yaml

# OS
.DS_Store
