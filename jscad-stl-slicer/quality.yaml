# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-jscad-stl-slicer,
# wave-4 batch-4c of the Phase D extension-quality backfill).
# jscad_stl_slicer.ts, jscad/stl_slicer.ts, and manifest.yaml are BYTE-FROZEN
# — every suite drives StlSlicer.<fn>(bytes, ...) directly (geometry seam) or
# model.methods.<m>.execute(args, ctx) against a fake context (writeResource +
# createFileWriter + definitionRepository + dataRepository) with real
# Deno.readFile over byte-accurate synthetic fixtures written by
# fixtures/builders.ts — no FS stubbing.
#
# Role map (one file per suite, no scaffolder-dumped ambiguity):
#   contract-fixture         -> jscad_stl_slicer_contract_test.ts
#   methods                  -> jscad_stl_slicer_methods_test.ts
#   adversarial               -> jscad_stl_slicer_adversarial_test.ts
#   coverage                  -> jscad_stl_slicer_coverage_test.ts
#   property-invariant-flow  -> jscad_stl_slicer_property_test.ts
schemaVersion: 1
extension: jscad-stl-slicer
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/jscad_stl_slicer_contract_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/jscad_stl_slicer_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/jscad_stl_slicer_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/jscad_stl_slicer_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/jscad_stl_slicer_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
# soak: jscad-stl-slicer's test task authority is not broad — no
# permission-narrowing override is needed. This block exists ONLY to declare
# `runs`: jscad-stl-slicer's local test:soak has always run at 5000, not the
# 10000 default (see scripts/quality/generate_soak_task.ts and
# STANDARD.md's "soak:" block section). Tracked in
# ext-quality-release-watch-soak (Phase B).
soak:
  state: na
  justification: jscad-stl-slicer's test task authority is not broad — no permission narrowing needed; this block exists only to declare a non-default local run count
  runs: 5000
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: jscad-stl-slicer bundles no Claude skill — a geometry slicer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
