# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-anime-cron, wave
# 2c of the Phase D extension-quality backfill). anime_cron.ts is
# unmodified — every new suite drives model.methods.<m>.execute() (and, in
# the contract-fixture suite, the pure exported helpers) against a stubbed
# fetch + stubbed Deno.Command + fake context, characterizing already-shipped
# behavior.
schemaVersion: 1
extension: anime-cron
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/anime_cron_test.ts
  methods:
    state: present
    files:
      - extensions/models/anime_cron_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/anime_cron_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/anime_cron_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/anime_cron_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: anime-cron bundles no Claude skill — a bare fetch/cron pipeline model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 0
  label: >-
    UNSCORABLE — `swamp extension quality manifest.yaml --json` prints a
    valid 92% (13/14, rubricVersion 3) score to stdout but exits 1 because
    symbols-docs is unearned (50% of exported symbols carry JSDoc, below the
    80% threshold); score_ratchet.ts's readScoreViaSwamp throws on the
    non-zero exit, so runRatchet reports anime-cron SKIPPED, not a CI
    failure. anime_cron.ts is byte-frozen here so the 50%->80% JSDoc
    remediation is out of scope. Tracked in workspace-ratchet-scorer-blockers
    (a distinct cause from seanime's broken-upgrade-chain unscorability —
    do not conflate the two).
