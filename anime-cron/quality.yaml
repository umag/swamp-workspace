# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-anime-cron, wave
# 2c of the Phase D extension-quality backfill). Every suite drives
# model.methods.<m>.execute() (and, in the contract-fixture suite, the pure
# exported helpers) against a stubbed fetch + stubbed Deno.Command + fake
# context.
#
# NOTE: the wave-2c backfill left anime_cron.ts byte-frozen and every suite
# purely characterizing. That is no longer true as of 2026.08.08.1, which
# added the fetch-archive method — that method's cases are real tests for new
# behavior. Everything predating it remains characterization.
schemaVersion: 1
extension: anime-cron
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/anime_cron_test.ts
  methods:
    state: present
    files:
      - extensions/models/anime_cron_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/anime_cron_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/anime_cron_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/anime_cron_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: na
    justification: anime-cron bundles no Claude skill — a bare fetch/cron pipeline model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: >-
    Grade A as of 2026.08.22.1 — NOW SCORABLE. `swamp extension quality
    manifest.yaml --json` reports 100%, rubricVersion 3, allPassed true,
    exit 0. The long-standing symbols-docs blocker described in the previous
    label is CLOSED: the remaining undocumented exports (parseResolution,
    groupScore, buildMagnet, NyaaHit, pickBest) gained JSDoc in this release,
    taking the ratio past the 80% threshold. score_ratchet.ts's
    readScoreViaSwamp no longer throws, so anime-cron is scored rather than
    SKIPPED. The only unearned factor is repository-verified (2 pts), which
    the registry awards on publish and no local run can earn. Stamped from
    the tool's own output, not guessed. Caveat: scored IN PLACE, not from a
    fresh worktree — the package cache can mask validation failures, so
    re-score from a clean checkout if this ever regresses.
