# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-musicbrainz,
# wave-2b full-build child of ext-quality-test-backfill). musicbrainz.ts was
# unmodified at that backfill; it has SINCE been modified (2026.08.02.1,
# musicbrainz-ssrf-and-latent-bugs) to real-fix LB2-LB7 — every suite now
# covers a mix of unmodified-behavior pins and fix-asserting tests, still
# against a host-routed stubbed fetch (musicbrainz.org JSON + *.bandcamp.com
# HTML) and a fake context.
schemaVersion: 1
extension: musicbrainz
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/musicbrainz_test.ts"
      - "extensions/reports/discography_sync_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/musicbrainz_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/musicbrainz_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/musicbrainz_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/musicbrainz_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: musicbrainz bundles no Claude skill — a search/lookup/browse + scraping model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
