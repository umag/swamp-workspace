# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-cozystack-linstor,
# wave-1 child of the Phase D extension-quality backfill, porkbun full-build
# recipe). cozystack_linstor.ts is unmodified — every suite drives
# model.methods.<m>.execute() / model.checks[<n>].execute() against a
# stubbed Deno.Command + fake context, characterizing already-shipped
# behavior (including two documented fail-open gaps, tracked separately as
# cozystack-linstor-fail-open-guards).
schemaVersion: 1
extension: cozystack-linstor
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/cozystack_linstor_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/cozystack_linstor_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/cozystack_linstor_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/cozystack_linstor_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/cozystack_linstor_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: cozystack-linstor bundles no Claude skill — a kubectl/linstor CLI wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
