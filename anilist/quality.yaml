# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-anilist, wave-2c
# full-build child of the Phase D extension-quality backfill). anilist.ts is
# unmodified — every suite drives model.methods.<m>.execute() against a
# stubbed fetch, characterizing already-shipped behavior over the public
# AniList GraphQL API and the ClickHouse charting sink.
schemaVersion: 1
extension: anilist
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/anilist_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/anilist_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/anilist_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/anilist_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/anilist_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: anilist bundles no Claude skill — a bare GraphQL wrapper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 92
  label: "92% — symbols-docs (JSDoc coverage) is the only unearned factor; anilist.ts is byte-frozen by this change so it cannot be added here"
