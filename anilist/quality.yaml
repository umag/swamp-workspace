# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-anilist, wave-2c
# full-build child of the Phase D extension-quality backfill). Every suite
# drives model.methods.<m>.execute() against a stubbed fetch. As of
# `2026.08.02.1`, AL1-AL4 (see CHANGELOG.md) are real fixes to gql()'s
# request/retry path; the adversarial suite's six re-pinned assertions now
# verify the fixed behavior instead of characterizing the bug.
#
# `2026.08.17.1` reconciled the forked main-repo copy back into this package.
# The adversarial suite is what CAUGHT the fork: its six AL1-AL4 pins failed
# against the main-repo source, proving production never had those guards. Keep
# running the full battery against any imported copy before trusting it.
schemaVersion: 1
extension: anilist
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/anilist_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/anilist_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/anilist_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/anilist_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/anilist_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: anilist bundles no Claude skill — a bare GraphQL wrapper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "100% (14/14, allPassed) — measured via `swamp extension quality anilist/manifest.yaml --json` after the 2026.08.17.1 fork reconcile; symbols-docs earned by JSDoc'ing the exported symbols in anilist.ts, including the status-change helpers added in 2026.08.17.1"
