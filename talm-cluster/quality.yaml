# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-talm-cluster,
# wave-1 full-build). talm_cluster.ts is unmodified — every suite drives
# model.methods.<m>.execute() / model.checks.<c>.execute() against a stubbed
# globalThis.Deno.Command + fire-immediately setTimeout, characterizing
# already-shipped behavior.
schemaVersion: 1
extension: talm-cluster
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/talm_cluster_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/talm_cluster_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/talm_cluster_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/talm_cluster_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/talm_cluster_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: talm-cluster bundles no Claude skill — a bare Talos CLI lifecycle wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
