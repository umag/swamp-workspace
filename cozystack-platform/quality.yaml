# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-cozystack-platform,
# wave-1 full-build child of ext-quality-test-backfill, mirroring the
# ext-quality-bf-porkbun pilot / PR #65). cozystack.ts is unmodified — every
# suite drives model.methods.<m>.execute() and model.checks.<c>.execute()
# against a stubbed, stateful Deno.Command and a fake context, characterizing
# already-shipped behavior.
schemaVersion: 1
extension: cozystack-platform
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/cozystack_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/cozystack_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/cozystack_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/cozystack_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/cozystack_property_test.ts"
watch:
  state: backlog
  justification: no generalized release-watch yet — tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: no live-canary workflow yet — tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: cozystack-platform bundles no Claude skill — a kubectl/helm wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
