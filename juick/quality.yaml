# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-juick, wave 2c
# full-build child of ext-quality-test-backfill); juick was re-classified
# from a scraper to a JSON API client during planning (plan v1 was rejected
# on exactly this point) — see CHANGELOG.md. The wave-2c backfill drove
# model.methods.<m>.execute() against a stubbed fetch + fake context,
# characterizing already-shipped behavior including 8 latent bugs
# (juick-latent-bugs). Re-stamped in 2026.08.02.1: all 8 are now FIXED
# (pins flipped in juick_adversarial_test.ts) — see CHANGELOG.md. Ratchet
# re-verified via `swamp extension quality manifest.yaml --json` and
# unchanged (still Grade A / 100% / 14/14, rubricVersion 3).
schemaVersion: 1
extension: juick
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/juick_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/juick_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/juick_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/juick_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/juick_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: juick bundles no Claude skill — a bare JSON API client model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
