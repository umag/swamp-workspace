# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-dawarich, wave-2c
# of the Phase D extension-quality backfill). dawarich.ts is byte-frozen —
# every suite drives model.methods.<m>.execute() against a stubbed fetch +
# fake context, characterizing already-shipped behavior.
schemaVersion: 1
extension: dawarich
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/dawarich_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/dawarich_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/dawarich_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/dawarich_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/dawarich_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: dawarich bundles no Claude skill — a REST API wrapper, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
