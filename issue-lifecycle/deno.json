{
  "tasks": {
    "test": "deno test --allow-read=. --allow-env=FC_NUM_RUNS extensions/models/",
    "test:soak": "FC_NUM_RUNS=10000 deno test --allow-read=. --allow-env=FC_NUM_RUNS extensions/models/issue_lifecycle_property_test.ts",
    "check": "deno check extensions/models/*.ts",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint"
  },
  "imports": {
    "zod": "npm:zod@4"
  },
  "fmt": {
    "lineWidth": 80,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
