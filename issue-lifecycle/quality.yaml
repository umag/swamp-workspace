# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-issue-lifecycle
# (wave-4 batch-4d, FINAL batch of the extension quality program).
# issue_lifecycle.ts (1881 LOC, pure logic — no fetch/subprocess/file I/O; a
# 13-state x 20-method state machine) and manifest.yaml are BYTE-FROZEN by
# this change — every listed test file characterizes/pins already-shipped
# behavior; no production code changed and no model/version bump.
#
# Role map (the scaffolder originally dumped both pre-existing test files
# under contract-fixture, which is correct here — they stay there; the four
# new suites below are additive, not a redistribution):
#   contract-fixture        -> issue_lifecycle.test.ts (pre-existing, 52
#                               tests, kept verbatim; also the fake-context
#                               harness every new suite file below reuses as
#                               its own copy) + issue_lifecycle_docs.test.ts
#                               (pre-existing model<->skill-docs drift guard)
#   methods                  -> issue_lifecycle_methods_test.ts (new)
#   adversarial               -> issue_lifecycle_adversarial_test.ts (new;
#                               also pins IL-1..IL-5 + IL-7, see the LOCAL
#                               issue-lifecycle-latent-bugs issue-lifecycle
#                               model — never the swamp.club Lab)
#   coverage                  -> issue_lifecycle_coverage_test.ts (new)
#   property-invariant-flow  -> issue_lifecycle_property_test.ts (new;
#                               pins IL-6 via frozen-clock exclusion)
schemaVersion: 1
extension: issue-lifecycle
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/issue_lifecycle.test.ts
      - extensions/models/issue_lifecycle_docs.test.ts
  methods:
    state: present
    files:
      - extensions/models/issue_lifecycle_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/issue_lifecycle_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/issue_lifecycle_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/issue_lifecycle_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/issue-lifecycle/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
