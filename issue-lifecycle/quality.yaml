# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Five-suite compliance shipped by ext-quality-bf-issue-lifecycle (wave-4
# batch-4d) and carried forward by the 2026.08.02.1 latent-bug real-fix.
# issue_lifecycle.ts (1881 LOC, pure logic — no fetch/subprocess/file I/O; a
# 13-state x 20-method state machine) now ships four real fixes (IL-1, IL-2,
# IL-4, IL-7) with model version bumped 2026.07.16.2 -> 2026.08.02.1 via an
# identity `upgrades[]` entry (no globalArguments/resource-schema change).
# Three latent bugs (IL-3, IL-5, IL-6) are explicitly re-affirmed as
# intentional behavior, not fixed — see the LOCAL issue-lifecycle-latent-bugs
# issue-lifecycle model for the full triage (never the swamp.club Lab).
#
# Role map (the scaffolder originally dumped both pre-existing test files
# under contract-fixture, which is correct here — they stay there; the four
# suites below are additive, not a redistribution):
#   contract-fixture        -> issue_lifecycle.test.ts (pre-existing, 52
#                               tests, kept byte-identical; also the
#                               fake-context harness every other suite file
#                               reuses as its own copy) + issue_lifecycle_
#                               docs.test.ts (pre-existing model<->skill-docs
#                               drift guard)
#   methods                  -> issue_lifecycle_methods_test.ts
#   adversarial               -> issue_lifecycle_adversarial_test.ts —
#                               asserts the FIXED behavior for IL-1/2/4/7 and
#                               re-affirms IL-3/5 as by-design
#   coverage                  -> issue_lifecycle_coverage_test.ts (incl. the
#                               new failingReviewers() pure-function coverage)
#   property-invariant-flow  -> issue_lifecycle_property_test.ts — P5
#                               rewritten for the fixed approve_plan gate;
#                               P4 re-affirms IL-6 via frozen-clock exclusion
schemaVersion: 1
extension: issue-lifecycle
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/issue_lifecycle.test.ts
      - extensions/models/issue_lifecycle_docs.test.ts
  methods:
    state: present
    files:
      - extensions/models/issue_lifecycle_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/issue_lifecycle_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/issue_lifecycle_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/issue_lifecycle_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/issue-lifecycle/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
