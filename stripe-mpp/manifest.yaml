manifestVersion: 1
name: "@magistr/stripe-mpp"
version: "2026.07.21.1"
description: "Stripe Machine Payments Protocol (MPP) — agent buyer (probe/mint/pay over HTTP-402 Payment auth), consumer buyer (grant a Shared Payment Token from a Link wallet via link-cli, spend by reference), and full seller API (challenge/verify/charge/receipt/reconcile/refund) for fiat Shared Payment Tokens, built on mppx and the Stripe preview SDK"
repository: "https://github.com/umag/swamp-workspace"
paths:
  base: manifest
models:
  - extensions/models/stripe_mpp.ts
additionalFiles:
  - README.md
  - LICENSE.md
skills:
  - stripe-mpp
platforms: []
labels:
  - stripe
  - mpp
  - payments
  - machine-payments
  - agentic-commerce
  - http-402
