# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-skype, wave-4
# batch-4a of the Phase D extension-quality backfill). Every suite drives
# model.methods.<m>.execute() against a stubbed Deno.Command (the sqlite3
# subprocess) + a real filesystem (listProfiles' Deno.readDir/Deno.stat walk,
# importToObsidian's vault writes) + fake context. As of 2026.08.02.1,
# skype.ts is no longer BYTE-FROZEN for latent bugs #3-#9 (real-fixed, pins
# flipped to green FIXED assertions) — LB1/LB2's 2026.08.01.1 fixes and every
# benign/frozen contract, methods, and coverage assertion remain
# byte-identical throughout.
#
# Role map (each test file assigned to exactly ONE honest role, per
# STANDARD.md's "named by ROLE, never by filename convention"):
#   contract-fixture        -> skype_test.ts
#   methods                 -> skype_methods_test.ts
#   adversarial              -> skype_adversarial_test.ts
#   coverage                 -> skype_coverage_test.ts
#   property-invariant-flow -> skype_property_test.ts
schemaVersion: 1
extension: skype
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/skype_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/skype_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/skype_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/skype_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/skype_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: skype bundles no Claude skill — a chat-history importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
