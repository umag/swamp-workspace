{
  "tasks": {
    "check": "deno check extensions/models/jabber_history.ts",
    "test": "deno test --allow-read --allow-write --allow-env=FC_NUM_RUNS extensions/models/ --permit-no-files",
    "test:soak": "FC_NUM_RUNS=2000 deno test --allow-read --allow-write --allow-env=FC_NUM_RUNS extensions/models/jabber_property_test.ts",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint"
  },
  "imports": { "zod": "npm:zod@4" },
  "compilerOptions": { "noImplicitAny": false },
  "fmt": {
    "lineWidth": 80,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": { "rules": { "tags": ["recommended"] } }
}
