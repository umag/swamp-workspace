# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-jabber (wave-4
# batch-4a child of ext-quality-test-backfill). jabber_history.ts is
# BYTE-FROZEN by this change — every listed test file characterizes/pins
# already-shipped behavior (including 9 latent bugs tracked in the LOCAL
# jabber-latent-bugs issue-lifecycle model, never filed to the Lab); no
# production code changed and no manifest/model version bump. This model
# parses NO XML (Psi/Psi+ pipe-delimited + plain-text formats only) — the
# adversarial suite falsifies XXE applicability with a covered-negative.
#
# Role map (each test file assigned to exactly ONE suite):
#   contract-fixture        -> jabber_test.ts
#   methods                 -> jabber_methods_test.ts
#   adversarial              -> jabber_adversarial_test.ts
#   coverage                 -> jabber_coverage_test.ts
#   property-invariant-flow -> jabber_property_test.ts
schemaVersion: 1
extension: jabber
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/jabber_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/jabber_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/jabber_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/jabber_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/jabber_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: jabber bundles no Claude skill — an XMPP-history importer, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
