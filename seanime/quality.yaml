# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-seanime, wave-2a
# full-build child of the Phase D extension-quality backfill). seanime.ts is
# unmodified — every suite drives model.methods.<m>.execute() against a
# stubbed fetch + fake context, characterizing already-shipped behavior.
schemaVersion: 1
extension: seanime
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/seanime_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/seanime_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/seanime_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/seanime_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/seanime_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: seanime bundles no Claude skill — a bare REST/AniList wrapper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 0
  label: >-
    UNSCORABLE — swamp extension quality errors on seanime.ts's broken model
    upgrade chain (2026.04.05.1 -> 2026.04.05.2 declared against a
    2026.07.16.2 model version); this predates the backfill and seanime.ts is
    byte-frozen here so it cannot be repaired in this change. score_ratchet.ts
    reports an unscorable extension as SKIPPED, not a CI failure. Tracked in
    workspace-ratchet-scorer-blockers; do not attempt to fix the chain here.
