# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-seanime, wave-2a
# full-build child of the Phase D extension-quality backfill). Every suite
# drives model.methods.<m>.execute() against a stubbed fetch + fake context,
# characterizing already-shipped behavior — no method or schema in
# seanime.ts changed. seanime.ts's upgrades[] array WAS later repaired (see
# CHANGELOG.md "Unreleased" -- Upgrade-chain repair) to close the chain gap
# this file's ratchet label used to document as UNSCORABLE; see ratchet
# below for the current, real score.
schemaVersion: 1
extension: seanime
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/seanime_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/seanime_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/seanime_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/seanime_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/seanime_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: seanime bundles no Claude skill — a bare REST/AniList wrapper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: >-
    Grade A (14/14, 100%) — the broken model upgrade chain that made `swamp
    extension quality` error (upgrades[] terminated at 2026.04.05.2 against
    model version 2026.07.16.2) is fixed: seanime.ts now carries lineage-
    repair bridge entries for 2026.04.05.2 -> 2026.05.25.1 -> 2026.07.16.2
    (identity upgradeAttributes, no globalArguments/resource schema change
    across either hop — see the entries' descriptions for the exact commits).
    Un-blocks workspace-ratchet-scorer-blockers; `swamp extension push
    seanime/manifest.yaml --dry-run -y` and `swamp extension quality` both
    succeed from a cold cache.
