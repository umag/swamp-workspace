# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance (ext-quality-bf-seadex, wave-2b
# full-build child of the Phase D extension-quality backfill), then REAL-FIXED
# in 2026.08.02.1 (all 8 tracked latent bugs, 3 HIGH — see CHANGELOG.md).
# seadex.ts is no longer unmodified: every suite still drives
# model.methods.<m>.execute() against a stubbed fetch, but the tests that used
# to characterize a latent bug now assert the fixed behavior across BOTH
# upstream contracts (releases.moe Pocketbase + public AniList GraphQL).
schemaVersion: 1
extension: seadex
tests:
  contract-fixture:
    state: present
    files:
      - "extensions/models/seadex_test.ts"
  methods:
    state: present
    files:
      - "extensions/models/seadex_methods_test.ts"
  adversarial:
    state: present
    files:
      - "extensions/models/seadex_adversarial_test.ts"
  coverage:
    state: present
    files:
      - "extensions/models/seadex_coverage_test.ts"
  property-invariant-flow:
    state: present
    files:
      - "extensions/models/seadex_property_test.ts"
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - "README.md"
  changelog:
    state: present
    files:
      - "CHANGELOG.md"
  skill:
    state: na
    justification: seadex bundles no Claude skill — a bare Pocketbase/AniList wrapper model, nothing to document as a skill
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
