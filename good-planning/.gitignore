.swamp/
.swamp-sources.yaml
.DS_Store
node_modules/

# Cached extension content — pulled at runtime, not authored here.
# Allow only the skill that IS this extension's own contribution.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/good-planning/
.claude/settings.local.json
.claude/projects/

# Locally-created model instances (smoke tests, lifecycle records).
# Runtime artifacts at the workspace root, not the extensions/models/ source.
/models/

# Generated by swamp init / extension pull — runtime metadata, not source.
CLAUDE.md
extensions/models/upstream_extensions.json
