# Generated by scripts/quality/scaffold.ts — see STANDARD.md.
# This file is then HAND-MAINTAINED: the scaffolder never overwrites an
# existing quality.yaml, so edits (justifications, suite states) persist.
#
# Backfilled to full five-suite compliance by ext-quality-bf-good-planning
# (wave-4 batch-4b child of ext-quality-test-backfill). good_planning.ts
# (1657 LOC, pure logic — no fetch/subprocess/file I/O) and manifest.yaml are
# BYTE-FROZEN by this change — every listed test file characterizes/pins
# already-shipped behavior; no production code changed and no model/version
# bump.
#
# Role map (the scaffolder originally dumped the pre-existing test file
# under contract-fixture, which is correct here — it stays there; the four
# new suites below are additive, not a redistribution):
#   contract-fixture        -> good_planning.test.ts (pre-existing, kept
#                               verbatim; also the fake-context harness every
#                               new suite file below reuses as its own copy)
#   methods                  -> good_planning_methods_test.ts (new)
#   adversarial               -> good_planning_adversarial_test.ts (new;
#                               also pins GP-1..GP-8, see the LOCAL
#                               good-planning-latent-bugs issue-lifecycle
#                               model — never the swamp.club Lab)
#   coverage                  -> good_planning_coverage_test.ts (new)
#   property-invariant-flow  -> good_planning_property_test.ts (new)
schemaVersion: 1
extension: good-planning
tests:
  contract-fixture:
    state: present
    files:
      - extensions/models/good_planning.test.ts
  methods:
    state: present
    files:
      - extensions/models/good_planning_methods_test.ts
  adversarial:
    state: present
    files:
      - extensions/models/good_planning_adversarial_test.ts
  coverage:
    state: present
    files:
      - extensions/models/good_planning_coverage_test.ts
  property-invariant-flow:
    state: present
    files:
      - extensions/models/good_planning_property_test.ts
watch:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
canary:
  state: backlog
  justification: seeded offender at CI-gate rollout — backfill tracked in ext-quality-test-backfill
docs:
  readme:
    state: present
    files:
      - README.md
  changelog:
    state: present
    files:
      - CHANGELOG.md
  skill:
    state: present
    files:
      - .claude/skills/good-planning/SKILL.md
ratchet:
  rubricVersion: 3
  baselinePercentage: 100
  label: "Grade A"
